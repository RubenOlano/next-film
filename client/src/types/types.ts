export interface Data {
  data: {
    actors: any;
    poster: string;
    title: string;
    year: number;
  };
}

export interface Poster {
  imgUrl: string;
}

export interface Title {
  title: string;
}
